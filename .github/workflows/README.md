# CI/CD Workflows

Этот проект включает два GitHub Actions workflow:

## 1. Tests and Code Quality (`tests.yml`)

**Когда запускается:**
- При каждом push в ветки `main`, `master`, `develop`
- При создании Pull Request в эти ветки

**Что делает:**
- ✅ Запускает тесты на Python 3.8, 3.9, 3.10, 3.11
- ✅ Проверяет код линтером flake8
- ✅ Проверяет форматирование с black
- ✅ Генерирует отчеты о покрытии кода
- ✅ Загружает артефакты с отчетами

**Важно:** Если workflow показывает ошибки линтера, их нужно исправить перед следующим коммитом!

## 2. Daily Code Quality Report (`daily_report.yml`)

**Когда запускается:**
- Автоматически каждый день в 00:00 UTC (scheduled)
- Вручную через "Run workflow" с параметрами

**Что делает:**
- 📊 Анализирует код в указанной директории
- 📝 Генерирует текстовые и JSON отчеты
- 📦 Сохраняет отчеты в артефактах (30 дней)
- 🎯 Создает краткое резюме

**Как использовать:**
1. Перейдите в Actions → Daily Code Quality Report
2. Нажмите "Run workflow"
3. Выберите параметры (директория, формат)
4. Скачайте отчеты из артефактов

## Исправление ошибок линтера

Если workflow показывает ошибки:

```bash
# Установите зависимости
pip install -r requirements.txt

# Проверьте ошибки
flake8 src tests

# Исправьте форматирование
black src tests

# Запустите тесты
pytest -v
```

