# Code Quality Assessment Tool

![Tests](https://github.com/YOUR_USERNAME/YOUR_REPO/workflows/Tests%20and%20Code%20Quality/badge.svg)

> **Примечание**: Замените `YOUR_USERNAME/YOUR_REPO` на реальные значения вашего репозитория для корректной работы badge.

## 📚 Описание

### Зачем это нужно?

**Проблема:** При оценке студенческих работ преподаватели тратят много времени на ручную проверку качества кода. Нужно проверить соответствие стандартам, наличие документации, сложность функций, дублирование кода. Это занимает часы и часто приводит к субъективным оценкам.

**Решение:** **Code Quality Assessment Tool** автоматизирует процесс оценки качества Python кода, предоставляя объективные метрики и конкретные рекомендации по улучшению.

### Что делает инструмент?

**Code Quality Assessment Tool** — это инструмент для автоматического оценивания качества студенческого Python кода по набору метрик. Инструмент анализирует код и выставляет оценку на основе соблюдения стандартов PEP 8, цикломатической сложности, наличия документации и дублирования кода.

### Практическая ценность

- ⏱️ **Экономия времени**: Автоматическая оценка вместо ручной проверки
- 📊 **Объективность**: Единые критерии для всех студентов
- 🎯 **Конкретные рекомендации**: Студенты получают четкие указания, что улучшить
- 📈 **Трекинг прогресса**: Можно отслеживать улучшение качества кода со временем
- 🔄 **Интеграция в CI/CD**: Автоматическая проверка при каждом коммите

### Основной функционал

- ✅ **Анализ PEP 8** — проверка соответствия кода стандартам Python
- ✅ **Цикломатическая сложность** — оценка сложности функций
- ✅ **Покрытие docstring** — проверка наличия документации
- ✅ **Детекция дублирования** — поиск повторяющихся блоков кода
- ✅ **Генерация отчетов** — создание текстовых и JSON отчетов с рекомендациями
- ✅ **Автоматизация через CI/CD** — ежедневные отчеты и артефакты

### Область применения

Инструмент предназначен для:
- Преподавателей, оценивающих качество студенческого кода
- Студентов, желающих улучшить качество своего кода
- Команд разработки, поддерживающих стандарты кодирования
- Автоматизированной проверки кода в CI/CD пайплайнах

---

## 🚀 Установка

### Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаг 1: Клонирование репозитория

```bash
git clone <your-repo-url>
cd code-quality-assessment-tool
```

### Шаг 2: Создание виртуального окружения (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## 💻 Использование

### Базовый пример: Анализ одного файла

```bash
python -m src.main data/sample_code.py
```

**Вывод:**
```
================================================================================
CODE QUALITY ASSESSMENT REPORT
================================================================================
Generated: 2024-12-20 15:30:45

File: data/sample_code.py

Overall Score: 78.5/100
--------------------------------------------------------------------------------
METRICS:
  PEP 8 Compliance: 92.5%
  Average Complexity: 3.2
  Max Complexity: 6
  Docstring Coverage: 75.0%
  Code Duplication: 15.3%
  Lines of Code: 65
  Functions: 5
  Classes: 1

FUNCTION COMPLEXITY:
  complex_function: 6
  simple_function: 1

RECOMMENDATIONS:
  • Add docstrings to functions and classes (current coverage: 75.0%)
  • Reduce code duplication (current ratio: 15.3%)
================================================================================
```

### Анализ директории

```bash
python -m src.main src/ --output reports/analysis.txt
```

### Генерация JSON отчета

```bash
python -m src.main data/sample_code.py --format json --output report.json
```

**Пример JSON вывода:**
```json
{
  "file_path": "data/sample_code.py",
  "overall_score": 78.5,
  "pep8_score": 0.925,
  "complexity": {
    "average": 3.2,
    "max": 6,
    "functions": {
      "simple_function": 1,
      "complex_function": 6
    }
  },
  "docstring_coverage": {
    "coverage": 0.75,
    "functions_total": 5,
    "functions_with_docstring": 4
  },
  "recommendations": [
    "Add docstrings to functions and classes",
    "Reduce code duplication"
  ]
}
```

### Программное использование

```python
from src.analyzer import CodeAnalyzer
from src.reporter import ReportGenerator

# Инициализация
analyzer = CodeAnalyzer()
reporter = ReportGenerator()

# Анализ файла
results = analyzer.analyze_file("path/to/file.py")

# Генерация отчета
text_report = reporter.generate_text_report(results)
print(text_report)

# Получение краткого резюме
summary = reporter.generate_summary(results)
print(f"Score: {summary['score']}/100")
print(f"Grade: {summary['grade']}")
```

---

## 📊 Метрики оценки

Инструмент использует взвешенную систему оценки:

| Метрика | Вес | Описание |
|---------|-----|----------|
| PEP 8 Compliance | 30% | Соответствие стандартам Python |
| Cyclomatic Complexity | 30% | Сложность функций (обратная зависимость) |
| Docstring Coverage | 20% | Наличие документации |
| Code Duplication | 20% | Уровень дублирования (обратная зависимость) |

### Система оценок

- **90-100**: A (Excellent) — Отличное качество кода
- **80-89**: B (Good) — Хорошее качество кода
- **70-79**: C (Satisfactory) — Удовлетворительное качество
- **60-69**: D (Needs Improvement) — Требует улучшения
- **0-59**: F (Poor) — Низкое качество кода

---

## 📁 Структура проекта

```
.
├── src/                      # Исходный код
│   ├── __init__.py
│   ├── analyzer.py          # Основной модуль анализа
│   ├── reporter.py          # Генератор отчетов
│   └── main.py              # Точка входа
│
├── tests/                    # Тесты
│   ├── __init__.py
│   ├── test_analyzer.py     # Тесты анализатора
│   └── test_reporter.py     # Тесты генератора отчетов
│
├── data/                     # Примеры кода для анализа
│   └── sample_code.py
│
├── docs/                     # Документация
│
├── examples/                 # Примеры отчетов и артефактов
│   ├── sample_report.txt
│   ├── sample_report.json
│   └── directory_report.txt
│
├── scripts/                  # Вспомогательные скрипты
│
├── .github/
│   └── workflows/            # CI/CD workflows
│       ├── tests.yml         # Базовый workflow для тестов
│       └── daily_report.yml  # Креативный workflow для отчетов
│
├── README.md                 # Этот файл
├── requirements.txt          # Зависимости
└── .gitignore               # Игнорируемые файлы
```

---

## 🧪 Тестирование

### Запуск всех тестов

```bash
pytest tests/ -v
```

### Запуск с покрытием кода

```bash
pytest tests/ -v --cov=src --cov-report=html
```

После выполнения откройте `htmlcov/index.html` в браузере для просмотра отчета о покрытии.

### Запуск конкретного теста

```bash
pytest tests/test_analyzer.py::TestCodeAnalyzer::test_analyze_simple_file -v
```

---

## 🔄 CI/CD и автоматизация

Проект включает два GitHub Actions workflow:

### 1. Tests and Code Quality (`tests.yml`)

**Триггеры:**
- Push в ветки `main`, `master`, `develop`
- Pull requests в эти ветки

**Действия:**
- ✅ Проверка кода на Python 3.8, 3.9, 3.10, 3.11
- ✅ Линтинг с flake8 (критические ошибки должны быть исправлены)
- ✅ Проверка форматирования с black
- ✅ Запуск unit тестов
- ✅ Генерация отчетов о покрытии кода
- ✅ Загрузка артефактов с отчетами

**⚠️ Важно:** Если workflow показывает ошибки линтера, их нужно исправить перед следующим коммитом!

**Как исправить ошибки линтера:**
```bash
# Проверьте ошибки
flake8 src tests

# Исправьте форматирование автоматически
black src tests

# Запустите тесты
pytest -v
```

### 2. Daily Code Quality Report (`daily_report.yml`)

**Креативные возможности:**

- 📅 **Scheduled workflow** — автоматический запуск каждый день в 00:00 UTC
- 🎛️ **Workflow dispatch** — ручной запуск с параметрами:
  - Выбор директории для анализа
  - Выбор формата отчета (text/json)
- 📦 **Artifact upload** — сохранение отчетов на 30 дней
- 📊 **Автоматическая генерация** — создание отчетов для всех файлов в директории

**Использование:**

1. **Автоматический запуск:** Workflow запускается автоматически каждый день
2. **Ручной запуск:** 
   - Перейдите в раздел "Actions" на GitHub
   - Выберите "Daily Code Quality Report"
   - Нажмите "Run workflow"
   - Выберите параметры и запустите

**Результаты:**
- Отчеты сохраняются в артефактах workflow
- Доступны для скачивания в течение 30 дней
- Включают текстовые отчеты и JSON summary

---

## 📝 Примеры результатов и артефактов

### Реальные примеры отчетов

В папке `examples/` находятся примеры реальных отчетов, сгенерированных инструментом:

- `sample_report.txt` — текстовый отчет для одного файла
- `sample_report.json` — JSON отчет с кратким резюме
- `directory_report.txt` — отчет для целой директории

### Пример 1: Хороший код

```
File: good_code.py
Overall Score: 92.5/100

METRICS:
  PEP 8 Compliance: 98.0%
  Average Complexity: 2.5
  Max Complexity: 4
  Docstring Coverage: 100.0%
  Code Duplication: 2.1%

RECOMMENDATIONS:
  • Code quality is good! Keep up the good work.
```

### Пример 2: Код, требующий улучшения

```
File: needs_improvement.py
Overall Score: 58.3/100

METRICS:
  PEP 8 Compliance: 65.0%
  Average Complexity: 12.8
  Max Complexity: 25
  Docstring Coverage: 20.0%
  Code Duplication: 45.2%

RECOMMENDATIONS:
  • Improve PEP 8 compliance: check line lengths and formatting
  • Reduce cyclomatic complexity (average: 12.8)
  • Refactor 3 function(s) with high complexity
  • Add docstrings to functions and classes (current coverage: 20.0%)
  • Reduce code duplication (current ratio: 45.2%)
```

### Примеры артефактов из CI/CD

После запуска workflow `daily_report.yml` в артефактах будут доступны:

1. **quality_report.txt** — основной отчет по анализируемой директории
2. **sample_code_report.txt** — детальный отчет по примеру кода
3. **summary.json** — краткое резюме в JSON формате:

```json
{
  "directory": "data",
  "files_analyzed": 1,
  "average_score": 76.8,
  "average_grade": "C (Satisfactory)",
  "timestamp": "2024-12-20T14:23:15.123456"
}
```

Эти артефакты доступны для скачивания в течение 30 дней после запуска workflow.

---

## 🛠️ Требования

Все зависимости указаны в `requirements.txt`:

- `pytest>=7.0.0` — фреймворк для тестирования
- `pytest-cov>=3.0.0` — покрытие кода тестами
- `flake8>=4.0.0` — проверка стиля кода
- `black>=22.0.0` — форматирование кода

**Примечание**: Инструмент использует только стандартную библиотеку Python (ast, os, json и т.д.) для анализа кода, поэтому дополнительные зависимости не требуются.

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект создан в образовательных целях.

---

## 👤 Автор

Студент курса "Искусственный интеллект в образовании"

---

## 🙏 Благодарности

- Python Software Foundation за отличный язык программирования
- Сообщество разработчиков за инструменты и библиотеки
- Преподавателям курса за ценные рекомендации

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел Issues в вашем репозитории
2. Создайте новый issue с описанием проблемы
3. Обратитесь к преподавателю на практических занятиях

> **Примечание**: Замените ссылки на `YOUR_USERNAME/YOUR_REPO` на реальные значения вашего репозитория.

---

**Удачи с анализом качества кода! 🚀**

